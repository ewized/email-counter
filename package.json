{
  "name": "email-counter",
  "version": "1.0.0",
  "description": "Simple webservice to count unique emails",
  "scripts": {
    "test": "jest src/**.test.js",
    "build": "babel src/**.js -d dist",
    "start": "npm run build && node dist/index.js"
  },
  "author": "ewized",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.7.5",
    "@babel/core": "^7.7.5",
    "@babel/preset-env": "^7.7.6",
    "jest": "^24.9.0"
  }
}
